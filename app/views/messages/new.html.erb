<% provide(:title, 'Enviar Nova Mensagem!') %>
<% provide(:button_text, 'Enviar!') %>
<h1>Enviar mensagem para <%= User.find(@message.to).name %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>