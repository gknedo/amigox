<% if @group.have_user_as?(current_user, 'invites') %>
<div class="panel panel-primary">
  <div class="panel-heading">
    <h4 class="panel-title">
      <a data-toggle="collapse" href="#collapseAcceptInvite">Aceitar Convite</a>
    </h4>
  </div>
  <div id="collapseAcceptInvite" class="panel-collapse collapse">
    <div class="panel-body">
      <div class="row">
        <div class="col-sm-2">
          <%= link_to "Aceitar", invite_accept_group_path(@group, user: current_user), class: 'btn btn-success btn-block', data: {confirm: "Deseja mesmo participar deste grupo?"}, method: 'post' %>
        </div>
        <div class="col-sm-10">
          <p>Você foi convidado para participar deste grupo!</p>
        </div>
      </div>
    </div>
  </div>
</div>
<% end %>